class Note {

  constructor(title, body) {
    this.title = title
    this.body = body
  };

};

// notes = [

// ]

// return this.note

// interface.js file can load in precreated notes in place of a database